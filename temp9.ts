import { db } from './src/shared/db/index'; import { bpmn_definitions } from './src/shared/db/schema'; async function run() { const all = await db.select().from(bpmn_definitions); console.log(JSON.stringify(all, null, 2)); process.exit(0); } run().catch(console.error);
