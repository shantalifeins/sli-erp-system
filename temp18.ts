import * as dotenv from 'dotenv'; dotenv.config(); import { createClient } from '@supabase/supabase-js'; async function run() { const supabaseAdmin = createClient(process.env.VITE_SUPABASE_URL || '', process.env.SUPABASE_SERVICE_ROLE_KEY || '', { auth: { autoRefreshToken: false, persistSession: false } }); const { data, error } = await supabaseAdmin.auth.admin.updateUserById('f4e19a51-6630-4104-9f27-e34fea1cf977', { password: 'password123' }); if(error) { console.error('Error:', error.message); } else { console.log('Password reset successfully to password123'); } process.exit(0); } run().catch(console.error);
