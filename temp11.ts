import { db } from './src/shared/db/index'; import { bpmn_definitions } from './src/shared/db/schema'; import { eq } from 'drizzle-orm'; async function run() { await db.delete(bpmn_definitions).where(eq(bpmn_definitions.name, 'CS Evaluation Approval Workflow')); console.log('Deleted duplicate'); process.exit(0); } run().catch(console.error);
