require('dotenv').config(); import { db } from './src/shared/db/index'; import { bpmn_definitions } from './src/shared/db/schema'; import { eq } from 'drizzle-orm'; async function run() { const w = await db.select().from(bpmn_definitions).where(eq(bpmn_definitions.documentType, 'CS Evaluation')); console.log(JSON.stringify(w, null, 2)); process.exit(0); } run().catch(console.error);
